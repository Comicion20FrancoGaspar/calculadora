const sumar = function name(a,b) {
    return a + b
}

const restar = function name(a,b) {
    return a - b
}

const multiplicar = function name(a,b) {
    return a* b
}

const dividir = function name(a,b) {
    return a / b
}

module.exports = {
    sumar, restar, multiplicar, dividir

}